

// A pointer to an anonumous function.
// Indicates that the pointer is fointing to a function and that the block is the function-block.
() => {updateLanguage();}

// Window is a browser variable containing several functions that are defined and will be invoked
// by the browser at different times. 
window

window.onload = function (){}   // onload exists in window and can be redefined anywhere in the script.
window.onunload = function (){} // onunload exists in window and can be redefined anywhere in the script.

Module // Module is a global variable in a browser

